# 1. Use Node 22 LTS as the base image
FROM node:22

# 2. Set working directory inside container
WORKDIR /app

# 3. Copy package files and install dependencies first
COPY package*.json ./
RUN npm install

# 4. Copy the rest of the application code
COPY . .

# 5. Build the TypeScript project
RUN npm run build

# 6. Expose backend port (e.g., 5000)
EXPOSE 5100

# 7. Start the server (run compiled JS)
CMD ["node", "dist/index.js"]
